#include <iostream>
using namespace std;

int lis(int &arr, int start, int end) {
  int parent[end - start];
  
  for (int i = start; i < end; i++) {

  }
}

int lds(int &arr, int start, int end) {

}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  
  int N;
  cin >> N;
  int arr[N];
  for (int i = 0; i < N; i++) {
    cin >> arr[i];
  }
}